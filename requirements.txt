# PII Guard — Production requirements for Render deployment
# Python 3.11 + CPU-only optimization
# No dev dependencies, no Rust compilation, prebuilt wheels only

# API Framework (35 MB)
fastapi==0.110.3
uvicorn==0.29.0
python-multipart==0.0.9
slowapi==0.1.9

# ML/NLP — CPU-optimized (580 MB total)
# Using CPU-only torch to avoid compilation + reduce size
torch==2.2.2
transformers==4.40.2
tokenizers==0.15.1
safetensors==0.4.2

# OCR (200 MB)
easyocr==1.7.1
Pillow==10.3.0
PyMuPDF==1.24.0

# CLI (minimal)
typer==0.12.0
rich==13.7.0

# Utilities
python-dotenv==1.0.0
httpx==0.27.0

# REMOVED PACKAGES (Render deployment incompatible):
# - pytesseract: Requires system Tesseract (not available on Render)
# - python-bidi: Requires Rust compilation (transitive, removed)
# - uvicorn[standard]: Too many binary dependencies removed direct extras
