# PII Guard — environment variables
# Copy to .env and fill in values

# API
PORT=8000
HOST=0.0.0.0

# NER model (HuggingFace model ID or local path)
NER_MODEL=dslim/bert-base-NER
NER_THRESHOLD=0.85

# Feature flags
USE_NER=true
MAX_FILE_SIZE_MB=20

# Frontend
VITE_API_URL=http://localhost:8000
